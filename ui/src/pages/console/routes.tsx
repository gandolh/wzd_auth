/**
 * PLACEHOLDER — **brief 10 owns this file and replaces it.**
 *
 * The single contract between the two UI briefs: brief 10 exposes the whole
 * console as one element, and brief 09's router mounts it under `/console`.
 * Neither brief edits the other's files.
 *
 * Brief 10: replace this with the console's own nested routes — login,
 * accounts, grants, apps, sessions, audit. Everything you render should sit
 * inside a wrapper carrying `data-ward-surface="console"` so the console tokens
 * in `tokens.css` apply; that is what makes it visibly not the ordinary login
 * page, which your brief calls for on the grounds that someone reaching for
 * break-glass should know they have.
 */
export function ConsoleRoutes(): React.JSX.Element {
  return <p>Ward console — not built yet. Brief 10 replaces this file.</p>;
}
