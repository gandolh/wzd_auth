---
/**
 * Starlight's light/dark toggle, removed.
 *
 * See ThemeProvider.astro: these docs are light-only, so a control that appears
 * to switch themes would be a lie. Removing it is better than disabling it.
 */
---
