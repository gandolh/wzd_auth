/**
 * `@ward/client` — placeholder until brief 08 builds it out.
 *
 * The workspace carries a tsconfig and this file so the root `typecheck` and
 * `build` scripts resolve while the package is still empty. Brief 08 owns
 * everything under `client/` and replaces this.
 */
export {};
